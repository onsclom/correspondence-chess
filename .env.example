# Correspondence Chess Configuration

# Set to "false" to disable SSR-first mode (requires JavaScript)
# Default: true (forms work without JS)
PUBLIC_SSR_FIRST=true

# Set to "false" to disable progressive enhancement JS
# Default: true (adds drag-drop, better UX with JS)
PUBLIC_ENABLE_ENHANCEMENTS=true
